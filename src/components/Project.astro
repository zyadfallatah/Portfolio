---
import { cn } from "../utils/utils";
import ProjectData from "./project/projectData.astro";
import ProjectBannar from "./project/ProjectBanner.astro"

interface Props {
  project: Project
  isMirroredToRight?: boolean
}

const {isMirroredToRight, project} = Astro.props;
const {title, shortDescription, metadata, bannar, technologies} = project;
---

<div class={cn("flex flex-col items-center lg:items-start justify-center gap-6", isMirroredToRight ? "md:flex-row-reverse lg:ml-[13rem]" : "md:flex-row lg:mr-[10rem]")}>
  <ProjectData title={title} shortDescription={shortDescription} technologies={technologies}/>
  <div class="hidden lg:block w-16 z-10">
    <img src="/assets/hexagon.svg" alt="hexagon icon" >
  </div>
  <ProjectBannar />
</div>